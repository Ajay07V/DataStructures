package com.strings;

public class SortNoBuiltIn {
    public static void main(String[] args) {
        String input = "computer";
        char[] characters = input.toCharArray();
//now sort characters using some algorithm
    //    String output = new String(sorted_characters);
    }
}
